import { RightClickDisabledDirective } from './right-click-disabled.directive';

describe('RightClickDisabledDirective', () => {
  it('should create an instance', () => {
    const directive = new RightClickDisabledDirective();
    expect(directive).toBeTruthy();
  });
});
